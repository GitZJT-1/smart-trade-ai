# MinIO 图纸下载发现模式

## 触发信号
- 招标项目匹配率极低（<30%）
- 项目目录下无 `bf_ceo`/`sf_cap`/`shf_*` 等图纸子目录
- 邀请函（`Приглашение к отбору ... .docx`）中提及 MinIO

## 案例：1224501 ISO 六分部集采

- 清单 1255 项，初始匹配仅 285（22.7%）
- 邀请函第 35 行：「Чертежи загружены на MinIO Console」
- 凭据：Логин `iso-id338611-reader`，Пароль `Elwli9tV`

## 诊断步骤

1. 打开邀请函 `.docx`，搜索 `minio`（不区分大小写）
2. 找到登录凭据（Логин/Пароль）
3. 获取 MinIO Console URL（通常在邀请函上下文或之前的项目经验中）
4. 下载图纸后重新运行匹配引擎

## 1224501 共享库覆盖率分析（2026-08-11）

匹配引擎 v20 在 9 个共享图纸库（19,846 文件）上的表现：
- 646 个含 ч. 图号的未匹配项中，仅 10 个的 PN 在共享库中存在
- 636 个（98.5%）的图纸完全不在本地共享库中
- 结论：1224501 图纸主体在 MinIO，本地共享库仅覆盖少量共用件
