# TradeWin Windows Standalone — build-time dependencies
# Core deps (fastapi/uvicorn etc.) come from parent pyproject.toml
PySide6>=6.7.0,<7.0
pyinstaller>=6.0,<7.0
hermes-agent
# Explicit: setup.py write_hermes_config() uses yaml
pyyaml>=6.0,<7.0
# OCR — Tesseract Python wrapper (system binary tesseract.exe required separately)
pytesseract>=0.3.10
