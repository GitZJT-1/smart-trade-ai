# Foreign Trade Assistant — 使用说明书

外贸 AI 助手，从找客户到拿订单。

---

## 一、安装

### macOS / Linux

```bash
curl -fsSL https://raw.githubusercontent.com/chefroger/smart-trade-ai/main/scripts/install.sh | bash
```

### Windows

```powershell
powershell -ExecutionPolicy Bypass -Command "Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/chefroger/smart-trade-ai/main/scripts/install.ps1' -OutFile install.ps1; .\install.ps1"
```

### 启动

```bash
python server.py
# 浏览器打开 http://127.0.0.1:9119/trade
```

首次启动时需要配置 LLM API Key（在 Hermes 设置中完成）。

### Windows 桌面版（TradeWin）

除了浏览器方式，Windows 用户也可使用独立桌面版 `TradeWin.exe`：

- 单文件 `.exe`，双击即用，无需单独安装 Python
- 首次启动自动弹出向导：选择 LLM 提供商 → 填入 API Key → 自动安装 Hermes + Skills
- 界面与浏览器版功能对等：AI 聊天、客户管理、文档库、任务面板、许可证、系统设置
- 每条 AI 回复右上角附「📋 复制」按钮，一键写入剪贴板
- 支持最小化到系统托盘、开机自启动

构建方式见 [`windows-standalone/README.md`](windows-standalone/README.md)。

---

## 二、界面概览

界面分左右两栏。左边是侧边栏（深色）：公司选择器、工作台、获客引流、销售转化、工具、历史。右边是主内容区（浅色）：聊天对话、客户表格、任务面板、历史记录，根据你选择的菜单切换。

---

## 三、首次使用

### 创建公司

点击侧边栏顶部公司选择器旁边的 **+** 按钮，填写公司信息，点击「创建公司」。

创建后桌面会自动生成一个以公司名命名的文件夹，里面有报价单、合同、客户资料、产品规格、发票、物流单据、认证资质、营销素材、海关数据这些子文件夹。把相关文件放进去，AI 就能读。

### 配置 Agent 身份

创建公司后弹出的窗口里有个「Agent 身份」标签，在那里填上公司做什么产品、有什么优势、主要卖给哪些市场、喜欢什么沟通风格。这些信息会影响 AI 回复你的方式。

---

## 四、功能详解

### 工作台

打开 Trade 就是工作台。上面显示今天的定时任务状态（哪些完成了、哪些还没跑），下面是你最近的对话。在聊天框里说"早安简报"，AI 会给你生成当天的汇率、大宗商品行情、市场新闻和待跟进客户清单。

### 获客引流

**客户开发** — 粘贴 LinkedIn 链接或者一段客户信息，AI 会分析客户背景、生成开发信、给一个跟进计划。比如 Day 1 发什么、Day 3 追什么、Day 7 怎么跟。

**网站诊断** — 把阿里国际站产品链接或者独立站链接贴进去，AI 会打开浏览器看你的页面，从 SEO、技术栈、设计几个角度给优化建议。

**社媒营销** — 告诉 AI 你想在哪些平台发内容（Facebook/Instagram/TikTok/YouTube），他会帮你出一份内容日历，具体到每条帖子文案可以直接复制用。

**LinkedIn** — Profile 优化建议、公司主页内容策略、InMail 开发信模板，都在这里。

**海关数据** — 把海关数据 CSV 或 Excel 文件放进桌面对应文件夹，AI 自动从里面筛出目标采购商、分析他们的采购规律、按优先级排好放到你面前。

### 销售转化

**客户管理** — 表格视图里能看到所有客户的名称、等级、国家、联系方式、关联的文档库、最近跟进时间。A/B/C 三级分类。点击任何一个客户行，右边会滑出一个详情面板，能编辑基本信息、管理关联文档库、看最近的对话。

**文档库** — 自动关联桌面工作目录的文件夹。你先选一个文档库，然后提问，AI 会读里面的文件来回答。支持 PDF、Word、Excel、PPT、图片、CSV 这些格式。也可以手动添加其他本地目录。

**文档生成** — 告诉 AI 你需要什么文档（PPT 演示、Word 合同、Excel 报价单），他会生成一个完整的文件给你下载。

### 工具

**客户背调** — 输入邮箱、域名或者公司名就行。AI 会从邮箱平台注册记录、域名 WHOIS、企业邮箱验证、制裁名单、技术栈、LinkedIn 等多个角度交叉验证，最后给你一个风险评级和行动建议。

**定时任务** — 页面上半部分是任务模板，钩选你需要的、设好时间、写好 Prompt，点「批量生成」就激活了。下半部分是已经在跑的任务，能看到状态，也能手动点「立即执行」。每天早上的简报是默认必选的。

### 对话记录

所有聊天记录按时间倒序排着，可以搜索，点任一条就能回到当时的对话上下文，不需要的可以删。

---

## 五、常用操作

### 怎么提问

不用特别学什么，直接问就行。比如你想查一家公司的底细，说"帮我背调一下 ABC Company"就行，AI 知道该加载什么技能。需要用到文件就把文件放进桌面工作目录，AI 会自动去读。你也可以直接说"用 b2b-osint 查这家公司"来指定用哪个功能。

想看今天有什么任务在跑，在聊天框说"工作清单"或"今日任务"。

### 切换公司

侧边栏顶部下拉框选择公司。所有数据（客户/文档库/对话）按公司隔离。

---

## 六、数据存储

所有数据存储在本地，不上传任何服务器（除了调用 LLM API）：

| 数据 | 位置 |
|------|------|
| 数据库 | `~/.trade/data/trade.db` |
| 公司档案 | `~/.trade/companies/{公司名}/` |
| 桌面工作目录 | `~/Desktop/{公司名}/` |
| LLM 配置 | `~/.hermes/config.yaml` |
| API Key | `~/.hermes/.env` |

---

## 七、常见问题

**Q: 如何更换 AI 模型？**
运行 `hermes setup` 重新配置。

**Q: 如何备份数据？**
复制 `~/.trade/` 目录和桌面工作目录即可。

**Q: AI 分析的文档支持哪些格式？**
PDF、Word (.docx/.doc)、Excel (.xlsx/.xls)、PPT (.pptx/.ppt)、CSV、图片 (.jpg/.png)、Markdown、HTML、纯文本。

**Q: 定时任务不执行？**
确保 Gateway 已启动（Trade 启动时会自动拉起）。

**Q: 点击「系统升级」后页面短暂不可用？**
这是正常的。升级完成后系统会自动重启服务并刷新页面。重启机制已优化：新进程先启动、等旧进程释放端口后再接管（最多等 10 秒），避免旧进程未退出导致端口冲突。如果页面没有自动恢复（超过 30 秒），按 `Ctrl+Shift+R`（Windows）/ `Cmd+Shift+R`（macOS）强制刷新。

---

## 八、安全说明

这个工具运行时 AI 可以直接调用系统工具（读文件、搜索网页等），不需要每一步都等你点确认——因为外贸业务员通常不知道怎么判断指令是否安全。所以请一定只在本机或内网用，不要暴露到公网，API Key 保管好，定期备份数据。
